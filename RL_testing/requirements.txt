# Diep RL_testing — Python dependencies
# Requires Python 3.12+ (see diepcustom/pyproject.toml).
#
# Install from diepcustom/RL_testing:
#   python3.12 -m venv ../.venv
#   ../.venv/bin/python -m pip install -r requirements.txt
#
# Redis for ghost league weights runs separately (see start_redis.sh).

# --- RLlib / Ray (PPO, Tune, RLModule) ---
ray[rllib]>=2.10.0
pydantic>=2.0.0
wandb>=0.17.0

# --- PyTorch (RPPO_pipeline, DiepModelConfig) ---
torch>=2.0.0

# --- Environment (DiepCustomParallelEnv + RLlib PettingZoo wrapper) ---
gymnasium>=0.29.0
pettingzoo>=1.24.0
numpy>=1.23.0

# --- Ghost league weight store (model_store.py, random_pick.py) ---
redis>=5.0.0
safetensors>=0.4.0

# --- Env-runner capacity probe (resource_compute/env_runner_capacity.py) ---
psutil>=5.9.0

# --- Dev / unit tests (league_initialization, random_pick) ---
pytest>=7.0
